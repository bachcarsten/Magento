/*Event.observe(window,'load',initSliderHints,false);

function initSliderHints() {
    if ($$('div.navigation')[0]) {
        $$('div.navigation')[0].insert($('search_mini_form'));
        Element.removeClassName($$('.mini-search')[0],'no-display');
    }
}*/